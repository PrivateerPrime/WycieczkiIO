@model WycieczkiIO.Models.Zakwaterowanie

@{
    ViewData["Title"] = "Dodaj zakwaterowanie";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Create</h1>

<h4>Dodaj zakwaterowanie</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="AddAccommodation">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="ZakwaterowanieId" class="control-label">Adres</label>
                <select asp-for="ZakwaterowanieId" class ="form-control" asp-items="ViewBag.ZakwaterowanieId"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Dodaj zakwaterowanie" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}