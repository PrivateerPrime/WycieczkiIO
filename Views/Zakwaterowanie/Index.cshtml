@model IEnumerable<WycieczkiIO.Models.Zakwaterowanie>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Adres)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Nazwa)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Typ)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Adres.Ulica)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Nazwa)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Typ)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ZakwaterowanieId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ZakwaterowanieId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ZakwaterowanieId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
